# Mantis-BugTracer

## Install:

```bash
git clone <repo>
cd myApp
amplify init
Do you want to use an existing environment? (Y/n): Y
Choose env you would like to use: dev
Select the authentication method you want to use: AWS access keys
# Usar las keys que mando Lares para cada miembro en el grupo de WhatsApp
region:  us-east-1
amplify pull
```
